# 《独家童话》开发恢复状态

> 本文件是第二阶段恢复与交接的主基线。Git 状态、批次记录和验证报告仍是补充证据。

## 更新时间

- 最后更新时间：2026-07-22（Asia/Shanghai）
- 当前分支：`codex/phase2-real-data`
- 当前阶段：第二阶段真实业务数据闭环
- 当前状态：**代码/云端联调与 Web 闭环进行中；Android 真机待验；未发布就绪**
- `main`：未修改

## 当前执行位置

第二阶段代码已收口。Supabase 开发项目、迁移与 A/B/C 的部分真实 Web 联调已经开始；后续仅处理验证直接暴露的缺陷，并继续补齐真实权限矩阵、失败场景和 Android 真机验收。

## 已完成代码范围

### 数据与安全基础

- Supabase 配置、时间戳迁移、核心业务表、RLS、私有 Bucket 和受控 RPC 已存在于分支。
- Mock、Real 和无效 API 模式已明确隔离；未知模式失败关闭，不会回退 Mock。
- App 页面不应直接访问 Supabase Client，统一通过 API/Store。
- Real 模式下未接入的 AI、会员支付、PDF 文件生成、云备份、语音、位置和反馈附件均明确关闭，不模拟成功。

### 认证与会话

- 登录、注册、Session 恢复均验证服务端用户、Session 用户和提交邮箱的一致性。
- Profile 写入限制昵称、头像文字和头像 URL，并验证返回记录属于当前用户。
- 登出、账号变化和本地数据清理会增加 Session epoch、停止 Realtime 并清除内存业务状态。
- 本地 Storage 部分清理失败时仍会退出旧 Session，并返回失败 key。

### 情侣关系

- 邀请码格式和过期时间使用服务端返回真值。
- 绑定结果必须为 active，且当前用户必须是关系成员。
- 非 active、解绑或成员身份异常的关系不会继续暴露伴侣资料或加载情侣业务数据。
- 情侣资料更新结果验证当前用户成员身份。

### 日记与附件

- 日记列表、详情、创建、更新和删除验证情侣空间与作者归属。
- Real 日记列表与详情统一以 `createdAt` 格式化展示时间；不再把 Mock 专用 `date` 缺失误显示为“日期未提供”。
- 创建、更新和删除以服务端实际返回行为准，零行写入不会报告成功。
- 附件先上传，数据库失败或异常时仅清理本次新上传对象，不误删调用方已有对象。
- 删除日记后清理本人拥有的附件；清理不完整时明确返回补偿信息。

### 照片集与照片

- 相册列表、详情、创建和删除验证情侣空间、上传者和嵌套照片归属。
- 多图写入数量与返回记录逐项校验。
- 上传失败、数据库失败和异常路径只补偿清理本次新上传对象。
- 删除照片集后清理本人拥有的 Storage 对象，并明确报告部分清理失败。

### 纪念日

- 创建、更新和删除使用服务端返回真值。
- 日期、标题和 ID 有明确输入校验。
- “下一个纪念日”按最近一次实际发生时间计算，兼容每年重复与不重复记录。

### 自定义标签

- 列表、创建、更新和删除验证情侣空间归属。
- 创建结果验证当前创建者。
- 标签名称、分类和图标增加输入边界。

### 时光胶囊

- 读取、创建、更新、删除和提醒 RPC 均要求有效情侣关系。
- 返回记录验证当前情侣空间和目标 ID。
- 创建结果验证当前创建者。
- 未解锁胶囊正文在 API 边界强制清空，防止进入 Store、缓存或调试工具。

### 评论与通知

- 评论目标必须真实存在并属于当前情侣空间。
- 评论列表和创建返回验证情侣空间、目标类型、目标 ID 和作者。
- 评论删除验证当前作者；内容限制 1000 字。
- 通知列表、单项已读和全部已读验证当前用户归属。
- 乐观更新失败只回滚本次目标，不覆盖其他并发成功状态。

### Realtime 与 Store

- Realtime 使用明确业务表白名单，不使用 `table: '*'`。
- 情侣数据按 `couple_id` 或情侣记录 ID 过滤，通知按当前用户 ID 过滤。
- 频道状态统一识别成功、错误、超时和关闭，并接入 Store 错误状态。
- Session 或情侣关系变化后旧频道、旧回调和延迟刷新均被清理或拦截。
- 服务端写入成功但后续刷新失败时，返回 `serverWriteConfirmed` 和 `refreshFailed`，不会误报写入失败导致重复提交。

### 页面真值与能力边界

- AI 视频配置、系统分享、PDF 配置/预览、云备份等页面已完成 Mock/Real 语义隔离。
- 系统分享区分真正分享、用户取消和调用失败。
- PDF 路由参数使用内容类型、封面、纸张和清晰度白名单；Real 模式不创建 Mock 导出任务。
- 云备份页面明确区分 Mock 演示、Real 未开放和无效配置。
- 搜索和时间线继续由已加载业务数据派生，不增加重复业务真相表。

## 当前验证证据

- 当前 HEAD：`2948e00`（`fix(diary): format real record timestamps in list`）。
- 当前 HEAD 的 Jest、`audit:pages`、`audit:real`、`audit:real-pages`、`audit:api`、`git diff main...HEAD --check`、Web export 与 Android 静态 export 均已通过。
- 以临时环境变量执行的 Mock 与无效 API 模式 Web export 均通过；无效模式的 fail-closed 语义另有静态测试覆盖，但其可视错误页仍待人工启动复验。
- `app/` 直接 Supabase Client 导入扫描为空；页面仍经 API/Store 访问 Real 数据。
- A/B 已完成注册、登录、邀请码绑定；A 创建日记后 B 能收到更新；A 上传私有照片后 B 能读取签名 URL；B 评论后 A 收到评论通知。
- 标签真实写入闭环已确认：在 A 会话创建“验收0719”后，页面分类数量从 0 变为 1；确认删除后恢复为 0，且无控制台错误或残留测试标签。
- C 已注册、登录且未绑定；已实际确认访问受保护业务路由会被前端守卫重定向到邀请码页。
- 首页日记统计此前指向不存在的 `/diary` 路由，已在当前 HEAD 新增真实日记列表页；已在真实登录会话中确认列表展示已同步日记，点按后进入具体详情并显示正文和标签。
- 日记列表 Real 数据时间展示缺陷已以自动化回归测试修复；由于当前未运行项目且 A/B 未登录，本次尚未重新进行浏览器可视复验。
- 2026-07-22 已重新以两份已绑定会话复验：两边均加载同一情侣空间的 5 篇日记和 1 组照片；`/diary` 正确显示真实日期，`/photo/album` 显示私有照片组。首次进入后约 15 秒才完成核心数据加载，数据未被删除；此加载体验仍需作为后续性能项处理。
- Android 静态 export 已于 2026-07-22 成功输出 196 个文件；这只证明 bundle 可导出，Android 真机验收仍待完成。

## 测试与批次记录

- 2026-07-22：首页已修复 Real 延迟模块加载期间误展示业务空数据的问题。启动阶段现在以模块请求态代替“0 条记录 / 0 篇日记 / 0 张照片”；已绑定真实会话刷新后仍显示 6 条记录、5 篇日记和 1 组照片。详见 `docs/resume-state-batches/2026-07-22-home-module-loading-empty-state.md`。
- 2026-07-22 第二轮 Web 复验：日记、相册、纪念日、标签和通知只读页面均取得真实会话结果；时光胶囊表单提交后仍卡在首屏确认加载，未将胶囊保存/未解锁正文边界记为通过。详见 `docs/resume-state-batches/2026-07-22-second-web-acceptance.md`。
- 2026-07-22：补齐情侣空间、我的、通知、搜索、标签和时光胶囊页面对 `loading.modules` 的请求态守卫，避免延迟模块加载期间误显示 0 数据。详见 `docs/resume-state-batches/2026-07-22-deferred-module-page-guards.md`。

- 每个 ChatGPT 修复批次均新增静态守卫测试。
- 详细批次记录位于 `docs/resume-state-batches/`。
- 详细真实联调与修复记录位于 `docs/resume-state-batches/`；不得以旧 HEAD 或静态检查替代当前 HEAD 的云端/设备验收。

## 当前未验证项目

以下均不能标记为已通过或发布就绪：

- Android 真机图片权限、键盘、Safe Area、Session 恢复、断网失败和核心双账号闭环（静态 export 已通过）
- Mock、Real、无效 API 模式的可视启动与错误页验证（仅构建和静态 fail-closed 语义已验证）
- A/B 情侣账号与 C 非成员账号的完整 RLS/Storage 权限矩阵（含反向读写与解绑状态）
- 双账号 Web 的日记列表/详情复验、纪念日、标签、胶囊、通知已读、刷新失败、重复提交与登出后 Realtime 清理
- Android 真机图片权限、键盘、Safe Area、Session 恢复和断网失败验证

## 外部阻塞

- 不得向 Git 提交 `.env`、Service Role、数据库密码或测试账号密码。
- Supabase Dashboard 已作为开发项目迁移与联调环境；CLI 连接曾发生 TLS EOF，迁移按安全替代流程在 Dashboard 执行。后续迁移或权限问题仍按单类失败最多两次的规则处理。

## Codex 下一步

1. 补齐 A/B/C RLS 与 Storage 反向权限矩阵，尤其是 C 读写拒绝、非上传者删除拒绝、解绑后拒绝与胶囊未解锁正文边界。
2. 补齐 A/B Web 的纪念日、胶囊、通知已读/全部已读、网络失败、重复提交、登出/切换账号及 Realtime 清理验证。
3. 修复验证暴露的问题，每个修复独立 Commit，并新增批次记录。
4. 在 Android 真机复验核心闭环；只有迁移、RLS/Storage、双账号 Web 和 Android 真机全部通过后，才可评估“发布就绪”。

## 协作规则

- 始终在 `codex/phase2-real-data` 工作，不修改 `main`。
- 不回滚已有 UI，不覆盖无关改动。
- 当前代码开发已收口；除验证发现缺陷外，不再扩展第二阶段范围。
