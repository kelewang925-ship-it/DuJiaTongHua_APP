# 2026-07-22 第二阶段第二轮 Web 验收

## 已确认

- A/B 已绑定会话的 `/diary` 显示 5 篇服务端日记，日期为 `2026/07/18`。
- `/photo/album` 显示 1 组私有照片。
- `/anniversary` 正常显示 0 个章节的真实空状态。
- `/tags` 正常显示 4 个分类及真实空状态。
- `/notifications` 显示 2 条服务端评论通知。

## 时光胶囊

- `/time-capsule/settings` 表单可加载，标题、未来日期、正文和内容类型控件均可见。
- 使用测试内容“第二阶段胶囊复验”、日期 `2099-01-01` 进行保存尝试后，页面跳转首页并长时间停留在“正在确认你的童话空间…”。
- 在约 40 秒观察窗口内未得到保存成功或已锁定胶囊的可视确认；浏览器错误日志为空。
- 本项不能记录为通过，也不能据此判定数据库写入失败。需在首屏加载问题改善或开发工具可查询到本次写入后复验胶囊锁定正文边界。

## 导航与网络失败观察

- 已绑定首页点击底部情侣空间入口后，页面进入加载态，随后显示“网络暂时走丢了”和重试入口。
- 这次未出现 Unmatched Route；失败态可见，但在当前网络状态下不能把导航实时成功记录为通过。

代码侧新增 `storeFailurePreservesCache.static.test.js`，确认模块请求失败只设置错误状态，不重置已加载的日记、照片和胶囊缓存。

## 未完成边界

- C 非成员真实 RLS/Storage 反向读写、解绑状态和深链最终可视结果仍待 C 会话。
- 通知单条/全部已读失败回滚、Session 切换和 Realtime 断开恢复仍未完成真实故障注入。
- Android 真机核心闭环仍待验；第二阶段不得标记发布就绪。
