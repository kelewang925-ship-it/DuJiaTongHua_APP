# 2026-07-17 通知归属校验批次

## 已完成

- 通知列表返回后验证每条记录均属于当前登录用户。
- 单项已读与全部已读返回增加 `user_id` 服务端确认。
- 发现缺少 ID 或用户归属不一致时失败关闭，不写入 Store。
- 增加静态守卫。

## 验证状态

当前 GitHub Connector 环境未执行测试、Web 构建、Android export 与三账号 RLS 集成测试，不记录为已通过。
