# 2026-07-18 第二阶段 Web 收尾复验

## 基线

- 分支：`codex/phase2-real-data`
- 当前 HEAD：`54a1731caaad9d746d83309ff78fef90c3fd6ec0`
- 范围：只记录第二阶段真实 Web 联调结果与本次路由缺陷修复，不扩展业务能力。

## 已确认的真实 Web 证据

- A/B 已完成邮箱注册、登录、邀请码绑定。
- A 写入日记后，B 的数据量更新；A 上传私有照片后，B 可通过签名 URL 读取；B 评论后，A 收到评论通知。
- C 已登录但未绑定，访问受保护业务路由时会转到邀请码页，未显示情侣业务数据。
- 首页照片统计可进入相册；首页日记统计原本指向不存在的 `/diary`，已由 `54a1731` 新增日记列表路由和详情入口。
- 在真实登录会话中，`/diary` 列表实际显示 5 条同步日记；点按“Realtime 再次验证4”进入 `/diary/detail?id=da947cc1-934c-47ef-a638-86831450e601`，详情显示正文、日期、标签、编辑和评论操作。
- 在 A 的真实登录会话中，创建测试标签“验收0719”后，心情分类从 0 枚变为 1 枚；通过确认删除移除后恢复为 0 枚。整个创建/删除过程无控制台错误，测试标签未残留。

## 当前 HEAD 本地门禁

- Jest 与 API/页面审计通过。
- `npm run check:web` 通过，Web 包已生成。
- 使用临时 `EXPO_PUBLIC_API_MODE=mock` 与无效值分别执行 Web export 均通过；无效值不会在构建时回退为 Mock，运行时可视错误页仍待人工复验。
- `git diff --check` 通过。
- `app/` 未直接导入 Supabase Client。

## 未完成，不能误报

- C 的完整 RLS/Storage 反向读写、解绑状态、胶囊锁定与通知伪造边界尚未完成。
- 纪念日、胶囊、通知已读、错误/重试、登出后 Realtime 清理尚未完成完整 Web 闭环。
- Android export 本轮未产生可验收文件；Android 真机验收仍待做。
- 第二阶段仍非发布就绪。
