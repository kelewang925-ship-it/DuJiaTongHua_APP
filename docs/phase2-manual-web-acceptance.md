# 《独家童话》第二阶段 Web 人工验收清单

> 仅记录真实浏览器和 Supabase 开发环境证据。代码、静态测试、Web export 和 Android export 不替代本清单。

## 已证实

- [x] A/B 已完成邀请绑定，只有一条 `active` couples 关系。
- [x] A 创建日记后，B 的日记数量通过 Realtime 更新；A 保存后进入详情页。
- [x] B 对 A 日记发表评论；A 收到“新的评论”通知，数据库触发器和接收者已核对。
- [x] A 上传照片后，B 的照片数量通过 Realtime 更新。
- [x] B 相册实际加载 A 的私有 Storage 签名 URL 图片；浏览器读取到自然尺寸 `640 × 427`，无图片读取错误。
- [x] B 尝试删除 A 的照片集被拒绝，数据未被删除。
- [x] 2026-07-22 新会话复验：A/B 均读取到 5 篇既有日记和 1 组既有照片；`/diary` 的 Real `createdAt` 已显示为实际日期，`/photo/album` 显示“Storage 验证”照片组。

> 注意：当前 Real 启动后核心数据约需 15 秒才完成加载。在加载层结束前不能以暂时空白或未渲染状态判断云端数据丢失。

## 当前修复后的首轮复验

使用 C（已登录、未绑定）与任一已绑定 A/B 会话执行。

| 项目 | 操作 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| C 深链保护 | C 访问 `/photo/album`、`/diary` 或任一业务 URL | 自动进入 `/account/invite`，不持续停在加载层，不显示情侣业务数据 | 待验 |
| 首页日记统计 | A 或 B 首页点击“日记”数量卡 | 进入 `/diary` | 待验 |
| 首页照片统计 | A 或 B 首页点击“照片”数量卡 | 进入 `/photo/album`，并显示已上传照片 | 待验 |

## 剩余双账号业务闭环

| 项目 | 操作 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| 情侣资料 | A 更新情侣资料，B 刷新或等待 Realtime | B 看到服务端真实资料；C 不可见 | 待验 |
| 标签 | A 创建一个自定义标签，B 查看/使用；C 尝试访问相关 URL | A/B 可见共享数据；C 被路由与服务端拒绝 | 待验 |
| 纪念日 | A 创建纪念日，B 查看、编辑或删除 | A/B 共同管理；时间线/首页数据一致 | 待验 |
| 时光胶囊 | A 创建未来日期胶囊，B 查看 | 未解锁时没有正文；到期后才可读取正文 | 待验 |
| 通知已读 | A 对已有评论通知分别单条已读、全部已读 | 页面和服务端状态一致；失败时不保留错误的乐观状态 | 待验 |
| 搜索与时间线 | A/B 对已创建日记、照片、纪念日搜索并查看时间线 | 结果由真实已加载数据派生，无 Mock 条目 | 待验 |
| Session 隔离 | A 登出、B 保持登录；随后切换会话 | 旧账号数据与 Realtime 不再更新到新会话 | 待验 |

## C 非成员权限矩阵

使用单独浏览器 Profile 登录 C。不要复制或分享 A/B 的会话、验证码、Token 或签名 URL。

| 项目 | 操作 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| 页面入口 | C 打开相册、日记、标签、纪念日、胶囊 URL | 前端回到邀请码流程 | 待验 |
| 直接 API/Storage | 仅通过受控测试工具尝试读取或写入 A/B 资源 | RLS/Storage Policy 拒绝，且无数据或对象残留 | 待验 |
| 已绑定关系 | C 输入无效、过期或已失效邀请 | 显示友好失败信息，不形成关系 | 待验 |

## 仍不能标记完成

- 上传/数据库写入失败补偿、Storage 删除失败和通知/胶囊并发回滚。
- Realtime 断开、超时、恢复及登出后清理。
- Android 真机图片权限、键盘、Safe Area、Session 恢复和断网失败。
- 以上未完成前，第二阶段不能标记“发布就绪”。
