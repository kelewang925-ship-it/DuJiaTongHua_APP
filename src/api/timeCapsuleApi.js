import { createApiError, createApiResponse, getAuthenticatedContext, isMockMode, requireCouple, requestMock } from './client';
import { fromDatabase } from './mappers';

export async function getTimeCapsules() { if (isMockMode()) return requestMock([]); try { const { supabase } = await getAuthenticatedContext(); const { data, error } = await supabase.rpc('get_time_capsules'); return error ? createApiError(error) : createApiResponse(fromDatabase(data || [])); } catch (error) { return createApiError(error); } }
export async function createTimeCapsule(payload) { if (isMockMode()) return requestMock({ id: `capsule_${Date.now()}`, ...payload }); try { const c = await getAuthenticatedContext(); requireCouple(c); const { data, error } = await c.supabase.from('time_capsules').insert({ couple_id: c.coupleId, creator_id: c.user.id, title: payload.title.trim(), content: payload.content.trim(), unlock_date: payload.unlockDate, reminder_enabled: payload.reminder ?? payload.reminderEnabled ?? true, content_types: payload.contentTypes || [] }).select('id').single(); return error ? createApiError(error) : createApiResponse({ id: data.id, ...payload, locked: true }); } catch (error) { return createApiError(error); } }
export async function updateTimeCapsule(id, payload) { if (isMockMode()) return requestMock({ id, ...payload }); try { const { supabase } = await getAuthenticatedContext(); const { data, error } = await supabase.rpc('update_time_capsule', { p_id: id, p_title: payload.title, p_content: payload.content, p_unlock_date: payload.unlockDate, p_reminder_enabled: payload.reminder ?? payload.reminderEnabled ?? true, p_content_types: payload.contentTypes || [] }); return error ? createApiError(error) : createApiResponse(fromDatabase(data)); } catch (error) { return createApiError(error); } }
export async function deleteTimeCapsule(id) { if (isMockMode()) return requestMock({ id, deleted: true }); try { const { supabase } = await getAuthenticatedContext(); const { error } = await supabase.rpc('delete_time_capsule', { p_id: id }); return error ? createApiError(error) : createApiResponse({ id, deleted: true }); } catch (error) { return createApiError(error); } }
export async function setTimeCapsuleReminder(id, enabled) { if (isMockMode()) return requestMock({ id, reminder: enabled }); try { const { supabase } = await getAuthenticatedContext(); const { error } = await supabase.rpc('set_time_capsule_reminder', { p_id: id, p_enabled: enabled }); return error ? createApiError(error) : createApiResponse({ id, reminder: enabled }); } catch (error) { return createApiError(error); } }
